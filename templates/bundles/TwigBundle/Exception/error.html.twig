{% extends 'base.html.twig' %}

{% block title %}Error - {{ status_code }} {{ status_text }}{% endblock %}
{% block body %}
    <div class="mt-5">
        <div class="row">
            <div class="col-lg-8 offset-2">
                <div class="article">
                    <div class="text-center">
                        <h2>Server has returned "{{ status_code }} {{ status_text }}".</h2>
                        <div class="mt-5 mb-5 svg-color">
                            {{ include('includes/svg/sad.html.twig') }}
                        </div>
                        <a class="btn btn-primary text-white" href="{{ path('article_index') }}">Home</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
