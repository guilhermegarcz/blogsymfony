{% extends "base.html.twig" %}

{% block title %}Edit tag {{ tag.id }}{% endblock %}
{% block body %}
    <div class="mt-5">
        <div class="row">
            <div class="col-lg-8 offset-2">
                <div class="article">
                    {{ form_start(form) }}

                    {{ form_widget(form) }}

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}