<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggle" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarToggle">
            <a class="navbar-brand" href="{{ path("article_index") }}">Blog</a>
            {% if is_granted("ROLE_ADMIN") %}
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_index') }}">Admin Area</a>
                    </li>
                </ul>
            {% endif %}
            <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
                {% if is_granted("ROLE_USER") %}
                    <li class="nav-item avatar">
                        <a class="nav-link">
                            <img data-src="holder.js/28x28?text={{ app.user.username|capitalize|first }}&bg=e83e8c&fg=fff&size=8" class="rounded-circle z-depth-0">
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled">{{ app.user.username }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("auth_logout") }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("auth_login") }}">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("auth_register") }}">Register</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
