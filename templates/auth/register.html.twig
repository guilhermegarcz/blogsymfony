{% extends 'base.html.twig' %}

{% block body %}
    <div class="mt-5">
        <div class="col-lg-4 offset-4">
            <div class="authform">
                <div class="row">
                    <div class="col-lg-12">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>

        </div>
    </div>

{% endblock %}